# Zerodha Clone

A full-stack Zerodha clone project.

## Project Structure

This project is organized into the following main directories:

- **frontend/**: The marketing/landing page of Zerodha, built with React.
- **dashboard/**: The core trading interface (Kite-like platform).
- **backend/**: The Node.js/Express API server handling authentication, orders, and data.

## Getting Started

### Prerequisites
- Node.js
- MongoDB

### Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/Sharan-git-web/zerodha-clone.git
   ```
2. Navigate to each directory and install dependencies:
   ```bash
   cd frontend && npm install
   cd ../dashboard && npm install
   cd ../backend && npm install
   ```

## Current Status
This branch (`projected-structure`) contains the base organizational structure and the landing page source code.
